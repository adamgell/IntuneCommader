# Rust
/target
**/*.rs.bk
Cargo.lock

# .NET
bin/
obj/
*.user
.vs/

# Release build artifacts (scripts/release.ps1) + local logs
/dist/
*.log

# Local data / secrets
*.db
*.sqlite
*.litedb
appsettings.*.local.json
.env

# OS / editor
.DS_Store
.idea/

# Claude Code personal overrides — machine-specific, never shared (the project-level
# .claude/settings.json and .claude/workflows/ ARE committed).
.claude/settings.local.json

# Smoke-test scratch — LOCAL ONLY. May contain real tenant data (GUIDs, device
# names). Never commit; only redacted exports in website/src/assets/screenshots/.
.smoke/

# Maester scan output — generated artifacts, may contain real tenant data.
test-results/
